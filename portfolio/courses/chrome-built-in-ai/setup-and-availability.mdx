---
title: "Setup & the availability lifecycle"
description: Enable the flags, understand the availability() states, monitor the model download, feature-detect, and degrade gracefully — the shared foundation every other API in the course reuses.
sidebar_position: 2
sidebar_label: Setup & availability
keywords:
  - Chrome AI flags
  - availability
  - Gemini Nano download
  - feature detection
  - graceful degradation
---

<LessonSchema
  name="Setup & the availability lifecycle"
  description="Enable the flags, understand the availability() states, monitor the model download, feature-detect, and degrade gracefully."
  path="/courses/chrome-built-in-ai/setup-and-availability"
/>

# Setup & the availability lifecycle

{/* Status: skeleton — beats only. */}

## Turning it on: flags and origin trials
## The four availability states
## Waiting for the model: monitor() and download progress
## Feature-detecting like you mean it
## Graceful degradation and the "enable this flag" banner
## Teardown discipline: destroy() and memory

---

Next: [The Prompt API](./prompt-api).
