---
title: "Generative UI (MCP Apps)"
description: Let a tool return an interactive UI, not just data — the ui.resourceUri wire format, a double-iframe sandbox, a JSON-RPC bridge, and app-only tools the model can't see but the iframe can call.
sidebar_position: 13
sidebar_label: Generative UI
keywords:
  - Generative UI
  - MCP Apps
  - SEP-1865
  - sandboxed iframe
  - postMessage
---

<LessonSchema
  name="Generative UI (MCP Apps)"
  description="Let a tool return an interactive UI, not just data — the ui.resourceUri wire format, a double-iframe sandbox, a JSON-RPC bridge, and app-only tools."
  path="/courses/chrome-built-in-ai/generative-ui"
/>

# Generative UI (MCP Apps)

{/* Status: skeleton — beats only. */}

## The tool-result-with-UI wire format
## Keeping ui:// out of the model's context
## The double-iframe sandbox model
## The JSON-RPC 2.0 postMessage bridge
## Hidden helper tools (visibility: ['app'])
## The bidirectional pick-without-a-round-trip flow

---

Next: [The MCP client in the browser](./mcp-client).
