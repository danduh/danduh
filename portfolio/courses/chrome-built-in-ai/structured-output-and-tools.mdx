---
title: "Structured output & tool calling"
description: Force the model to return JSON with responseFormat, wire real functions with the tools parameter, and learn the responseFormat intent-loop workaround for builds where native tool calling is unreliable.
sidebar_position: 4
sidebar_label: Structured output & tools
keywords:
  - structured output
  - responseFormat
  - tool calling
  - function calling
  - JSON schema
---

<LessonSchema
  name="Structured output & tool calling"
  description="Force JSON output with responseFormat, wire real functions with the tools parameter, and learn the responseFormat intent-loop workaround."
  path="/courses/chrome-built-in-ai/structured-output-and-tools"
/>

# Structured output & tool calling

{/* Status: skeleton — beats only. */}

## Constraining output with responseFormat (JSON Schema)
## Native tool calling: `create({ tools })`
## Writing an execute handler
## The responseFormat intent-loop workaround
## When to use which

---

Next: [Multimodal: image input](./multimodal).
