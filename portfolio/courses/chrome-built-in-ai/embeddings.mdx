---
title: "Embeddings (SemanticEmbedder)"
description: Turn text into on-device vectors with SemanticEmbedder — pick the right task type, compare with cosine similarity, shrink with Matryoshka truncation, and build semantic search with no backend.
sidebar_position: 11
sidebar_label: Embeddings
keywords:
  - Embeddings API
  - SemanticEmbedder
  - semantic search
  - cosine similarity
  - embeddinggemma
---

<LessonSchema
  name="Embeddings (SemanticEmbedder)"
  description="Turn text into on-device vectors — pick the right task type, compare with cosine similarity, shrink with Matryoshka truncation, and build semantic search with no backend."
  path="/courses/chrome-built-in-ai/embeddings"
/>

# Embeddings (SemanticEmbedder)

{/* Status: skeleton — beats only. */}

## Availability and waiting for the model
## Embedding one string and a batch
## Task types (and the query/document asymmetry)
## Comparing with cosine similarity
## Matryoshka dimensions
## Same embedding space only
## Building semantic search

---

Next: [WebMCP](./webmcp).
